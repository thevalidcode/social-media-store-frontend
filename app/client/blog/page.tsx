import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog",
  description: "Blog",
  keywords: ["blog", "crop studio", "crop", "studio"],
};

export default function BlogPage() {
  return (
    <div>
      <h1>Blog</h1>
    </div>
  );
}
